```cpp
/**
 * @file FIXME.md
 * @brief Архитектурный долг - чеклист
 * @note Статус: В процессе
 */
```
# FIXME.md 

## О структуре файла

**Раздел 1. Архитектурный рефакторинг** — содержит задачи, вытекающие из архитектурных решений, принятых в ARCH_REFACTORING_ROADMAP.md. Они требуют изменения структуры кода, но уже осмыслены и разбиты на конкретные шаги.

**Раздел 2. Исправление найденных ошибок** — содержит изолированные проблемы, не связанные с архитектурой: баги, несовместимости, недочёты интерфейса, которые можно исправить точечно, без перестройки слоёв.

**Раздел 3. Косметические улучшения** — задачи, не влияющие на функциональность: стили, предупреждения компиляции, улучшение читаемости кода.

## 1. Архитектурный рефакторинг

| # | Задача | Подзадачи | Статус | Приоритет |
|---|--------|-----------|--------|-----------|
| **1.1** | **Рефакторинг слоя провайдера данных** | Продумать оптимизацию слоя провайдера данных (варианты: единый провайдер, фабрика, три отдельных провайдера с `DeviceController*`) | ⬜ | 🔴 Критично |
| | | Выбрать и реализовать оптимальный вариант | ⬜ | |
| | | Убрать прямые вызовы `g_configManager` из провайдера | ⬜ | |
| | | Обновить `main.cpp` под новый провайдер | ⬜ | |
| **1.2** | **Перенести AP-провизионинг в `provisioning`** | Создать в `provisioning_manager.cpp` функцию `provisioning_handle_ap_save(server)` | ✅ | 🔴 Критично |
| | | Создать в `provisioning_manager.cpp` функцию `provisioning_send_ap_page(server)` | ✅ | |
| | | Убрать AP-страницу из `web_manager.cpp` | ✅ | |
| | | Обновить `web_manager.cpp` для вызова функций provisioning | ✅ | |
| | | Создать легковесный HTTP-сервер внутри `startApProvisioning()` | ✅ | |
|**1.2.2** | Вынести AP сервер из в `ap_server`| | ⬜ | |
| | | перенести AP сервер из `provisioning_manager` в `ap_server`| ⬜ | |
| | | удалить функционал AP сервера из `web_manager`| ⬜ | |
| **1.3** | **Вынести управление железом в `DeviceController`** | В `DeviceController::init()` инициализировать актуатор и датчик | ⬜ | 🔴 Критично |
| | | В `DeviceController::update()` обновлять актуатор и датчик | ⬜ | |
| | | Убрать `sensor_update()` из `main.cpp` | ⬜ | |
| | | Убрать `fan->update()` / `switchActuator->update()` из `main.cpp` | ⬜ | |
| **1.4** | **Абстракция актуаторов** | Объединить `FanActuator` и `SwitchActuator` в единый `Actuator` с заглушками | ⬜ | 🟡 Средний |
| | | Обновить `DeviceController` под единый актуатор | ⬜ | |
| | | Обновить `WebStatusProvider` под единый актуатор | ⬜ | |
| | | Обновить `main.cpp` под единый актуатор | ⬜ | |
| | | Удалить `actuator_base.*`, `fan_actuator.*`, `switch_actuator.*` | ⬜ | |
| **1.5** | **Транспортная абстракция** | Транспорт должен сам управлять публикацией при подключении | ⬜ | 🔴 Критично |
| **1.6** | **Инкапсуляция ProvisioningManager** | Создать глобальные обёртки для всех методов `ProvisioningManager`, чтобы оркестратор не знал о классе | ⬜ | 🟡 Средний |
| | | Создать `provisioning_update()` — обёртка для `ProvisioningManager::update()` | ⬜ | |
| | | Создать `getProvisioningData()` — обёртка для `ProvisioningManager::getProvisioningData()` | ⬜ | |
| | | Обновить `main.cpp` для использования обёрток | ⬜ | |
| **1.7** | **Остановка провизионинга без перезагрузки (как BLE)** | Оркестратор должен уметь останавливать провизионинг после получения данных | ⬜ | 🟡 Средний |
| | | Добавить метод `ProvisioningManager::stop()` для выхода из режима настройки | ⬜ | |
| | | При остановке ProvisioningManager должен: снять `STATE_PROVISIONING`, остановить AP-сервер, сбросить полученный логин/пароль | ⬜ | |
| | | Обычный Web-сервер должен работать когда провизионинг НЕ активен | ⬜ | |
| | | AP-режим: перезагрузка не требуется (как BLE) | ⬜ | |
| | | Обновить `main.cpp`: после сохранения вызывать `provisioning_stop()` вместо `restart_request()` | ⬜ | |
| | | Проверить работу WiFi/MQTT без перезагрузки после AP-провизионинга | ⬜ | |
| **1.8** | **Привести имена слоёв к единому стандарту** | | ⬜ | 🟡 Средний |
| 1.8.1 | | Переименовать `led.h/cpp` → `led_manager.h/cpp` | ✅ | |
| 1.8.2 | | Переименовать `ota.h/cpp` → `web_ota_manager.h/cpp` | ✅ | |
| 1.8.3 | | Переименовать `mqtt.h/cpp` → `mqtt_manager.h/cpp` | ✅ | |
| 1.8.4 | | Переименовать `web.h/cpp` → `web_server.h/cpp` | ✅ | |
| 1.8.5 | | Переименовать `zigbee.h/cpp` → `zigbee_manager.h/cpp` | ✅ | |
| 1.8.6 | | Переименовать `transport_mqtt.h/cpp` → `mqtt_transport.h/cpp` | ⬜ | |
| 1.8.7 | | Переименовать `transport_zigbee.h/cpp` → `zigbee_transport.h/cpp` | ⬜ | |
| 1.8.8 | | Переименовать `reset_btn.h/cpp` → `reset_button_manager.h/cpp` | ✅ | |
| 1.8.9 | | Переименовать `provisioning.h/cpp` → `provisioning_manager.h/cpp` | ✅ | |
| 1.8.10| | Переименовать `sensor.h/cpp` → `sensor_reader.h/cpp` | ⬜ | |
| 1.8.11| | Переименовать `ble_server.h` → `ble_provisioning_server.h` | ⬜ | |
| 1.8.12| | Переименовать `web_common.h/cpp` → `http_common.h/cpp` | ⬜ | |
| 1.8.13| | Переименовать `web_templates.h` → `html_templates.h` | [✅] | |
| 1.8.14| | Обновить все `#include` в проекте после переименований | ⬜ | |
| 1.8.15| | Обновить `platformio.ini` (если есть ссылки на файлы) | ⬜ | |
| | | | | |
| **1.9** | **Создать недостающую документацию для слоёв** | | ⬜ | 🟡 Средний |
| 1.9.1 | | Создать `LOGGER.md` (описание уровней, категорий, макросов) | ✅ | |
| 1.9.2 | | Создать `WDT_MANAGER.md` (описание WDT, таймауты, API) | ✅ | |
| 1.9.3 | | Создать `RESTART_MANAGER.md` (централизованная перезагрузка) | ✅ | |
| 1.9.4 | | Создать `RESET_BUTTON_MANAGER.md` (кнопка сброса, стадии нажатия) | ✅ | |
| 1.9.5 | | Создать `WIFI_MANAGER.md` (WiFi подключение, STA/AP, сканирование) | ✅ | |
| 1.9.6 | | Создать `DEBUG_TOOLS.md` (отладочные утилиты) | ✅ | |
| 1.9.7 | | Создать `CREDENTIALS.md` (заводские настройки) | ✅ | |
| 1.9.8 | | Обновить `ARCHITECTURE.md` (раздел 10) — добавить ссылки на новые документы | ✅ | |
| **1.10** | **Рефакторинг HTML-шаблонов** | | ⬜ | 🟡 Средний |
| 1.10.1 | | Убрать `sendConfigPage()` из `html_templates.h` — это логика Web-слоя | ⬜ | |
| 1.10.2 | | Перенести `sendConfigPage()` в `web_manager.cpp` как статическую функцию | ⬜ | |
| 1.10.3 | | Добавить параметр `bool showOtaButton` в `sendConfigPage()` | ⬜ | |
| 1.10.4 | | Убрать `#include "web_ota_manager.h"` из `html_templates.h` | ⬜ | |
| 1.10.5 | | Обновить вызов `sendConfigPage()` в `web_manager.cpp` с передачей `ota_is_available()` | ⬜ | |



## 2. Исправление найденных ошибок

| # | Задача | Подзадачи | Статус | Приоритет |
|---|--------|-----------|--------|-----------|
| **2.1** | **LEDC API несовместим с ESP32-C6** | Добавить условную компиляцию для ESP32-C6/H2 | ✅ | 🔴 Критично |
| **2.1** | | `fan_actuator.cpp` | ✅ | 🔴 Критично |
| **2.1** | | `wdt_manager.cpp` | ✅ | 🔴 Критично |
| **2.2** | **Сохранение конфигурации не происходит** | Исправить обработку `/save` в `web_manager.cpp` | ⬜ | 🔴 Критично |
| | | Убедиться, что `g_webConfigPending` обрабатывается в `main.cpp` | ⬜ | |
| **2.3** | **Интерфейс web-страницы** | Убрать лишние кнопки, добавить недостающие | ⬜ | 🔴 Критично |
| | | Исправить отображение текущего режима | ⬜ | |
| | | Исправить выравнивание иконок | ⬜ | |
| | | Добавить отказ от сохранения при снятой галке Confirm saving | ⬜ | |
| **2.4** | **Дублирование Zigbee** | Удалить `zigbee.h/cpp` | ✅ | 🟡 Средний |
| **2.5** | **Web не должен вызывать `sensor_*()` напрямую** | В `web_manager.cpp` заменить вызовы `sensor_*()` на методы `IWebStatusProvider` | ⬜ | 🟡 Средний |
| **2.6** | **Инкапсуляция слоев** | Убрать прямые вызовы `g_configManager` из `wifi_manager` | ✅ | 🟡 Средний |
| | | Убрать прямые вызовы `g_configManager` из `provisioning` (передавать `deviceId` через параметры в `ProvisioningManager::begin(deviceId)`) | ⬜ | 🟡 Средний |
| **2.7** | **Транспорт (MQTT)** | В `transport_mqtt.cpp` в `update()` добавить публикацию всего состояния при подключении | ⬜ | 🔴 Критично |
| **2.8** | **Актуализация ARCHITECTURE.md** | | ⬜ | 🟡 Средний |
| 2.8.1.| |Актуализировать состояние раздела DeviceController) | ⬜ | 🟡 Средний |
| **2.8** | **Cлой MQTT** | Нет публикации параметров и измененных состояний| ⬜ | 🟡 Средний |
| 2.8.1.| |Актуализировать DeviceController | ⬜ | 🟡 Средний |
| **2.9** | **Транспорт создаётся в режиме провизионинга** | Добавить проверку WiFi перед созданием транспорта | ⬜ | 🟡 Средний |
| | | В `main.cpp` создавать `g_transport` только если `strlen(wifiSsid) > 0` | ⬜ | |
| | | Добавить лог `"Skipping transport creation (provisioning mode)"` | ⬜ | |
| **2.11** | **Рефакторинг оркестратора (main.cpp)** | | ⬜ | 🔴 Критично |
| 2.11.1 | | Вынести логику определения режима LED в отдельную функцию `updateLedMode()` | ⬜ | |
| 2.11.2 | | Вынести логику провизионинга из `loop()` в отдельную функцию `handleProvisioning()` | ⬜ | |
| 2.11.3 | | Вынести логику потери WiFi в отдельную функцию `checkWiFiFallback()` | ⬜ | |
| 2.11.4 | | Вынести обработку команд от Web в отдельную функцию `handleWebCommands()` | ⬜ | |
| 2.11.5 | | Убрать прямые вызовы `g_configManager` из оркестратора (использовать колбэки) | ⬜ | |
| 2.11.6 | | Убрать `#ifdef` из оркестратора (использовать заглушки) | ⬜ | |
| 2.11.7 | | Создать `ORCHESTRATOR.md` с полным описанием ответственности | ✅ | |

## 3. Косметические улучшения

| # | Задача | Подзадачи | Статус | Приоритет |
|---|--------|-----------|--------|-----------|
| **3.1** | **HTML_STYLE** | Разделить на `HTML_STYLE_BASE` и `HTML_STYLE_EXTENDED` | ⬜ | 🟢 Низкий |
