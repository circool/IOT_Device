```cpp
/**
 * @file PROJECT.md
 * @brief Описание проекта
 * @note Статус: В процессе
 * @todo Актуализировать разделы и удалить лишние подробности
 * @todo Удалить/перенести разделы описанные в ARCHITECTURE.MD
 * @todo Удалить/перенести разделы описанные в ARCHITECTURAL_BASIS.MD
 */

```

# Универсальное устройство умного дома

## Описание проекта

### Цель создания

Устройство разработано для автоматического управления вытяжной вентиляцией в туалете и ванной комнате. В типовой квартирной разводке вытяжной вентилятор подключён к цепи освещения и включается одновременно со светом, работая всё время пока в помещении горит свет — без учёта реальной потребности в вентиляции. Устройство решает эту проблему: вентилятор включается автоматически только при превышении порогов температуры или влажности, а не при каждом использовании помещения.

## Основные возможности и режимы работы

Прошивка устройства универсальна и на этапе компиляции настраивается на один из трёх типов:

| Тип | Назначение | Ключевые компоненты |
|-----|-----------|---------------------|
| 1 | Вентилятор с датчиком | Датчик температуры/влажности + реле |
| 2 | Автономный датчик | Только датчик температуры/влажности |
| 3 | Управляемый выключатель | Реле, без датчика |

Каждый тип поддерживает набор функций, описанных в `DEVICE_TYPES.md` и различные каналы взаимодействия см. `TRANSPORT.md` 


### Описание режимов работы и функциональных особенностей

#### Общие для всех типов возможности

**Хранение настроек.** Все параметры конфигурации сохраняются в энергонезависимой памяти устройства и не теряются при отключении питания.

**Светодиодная индикация.** Встроенный светодиод отображает текущий статус: наличие соединения с WiFi и MQTT, режим настройки, аварийное отключение исполнительного механизма.

**Режим настройки** При первом включении (отсутствии настроек) или потере связи с настроенной сетью устройство переходит в режим настройки.
Режим поддерживает одновремненную работу 2 каналов настройки:
- WiFi точка доступа. Устройство создаёт собственную WiFi-сеть. Подключившись к ней, пользователь получает доступ к веб-интерфейсу для ввода параметров WiFi.
- BLE-комиссионинг. Для устройств поддерживающих пролокол Bluetooth Low Energy (BLE) доступна установка параметров WiFi. Доступен только если у устройства нет настроек WiFi/пароля или соединиться с настроенной сетью не удалось. 

**Среда носителя**
`TRANSPORT_TYPE` определяет физический носитель:

0 = NONE (автономный режим)
1 = WIFI (MQTT, Web, Matter over WiFi) для 8266/ESP32-C3/C6/H2/S3
2 = ZIGBEE (полный стек ZigBee) для ESP32-C6/H2
3 = THREAD (Matter over Thread) для ESP32-C6/H2

**Сброс настроек.** Предусмотрена возможность принудительного сброса всех параметров к значениям по умолчанию — по команде через канал взаимодействия или длительным нажатием аппаратной кнопки сброса.

**Обновление прошивки.** Поддерживается обновление «по воздуху» (OTA) через веб-интерфейс, без демонтажа устройства.

**Программный Watch Dog.** Аппаратный или программный сторожевой таймер отслеживает зависания устройства и выполняет автоматическую перезагрузку.

---

#### Для типов 1 и 3 (исполнительное устройство)

**Таймер аварийного отключения.** Принудительно отключает нагрузку, если она проработала непрерывно дольше заданного времени. Служит защитным механизмом, предотвращая бесконечную работу. Может быть отключён установкой нулевого значения.

**Начальное состояние при старте.** Определяет поведение реле сразу после подачи питания: включено или выключено. 

---

#### Для типа 1 (вентилятор, управляемый датчиком)

Устройство типа 1 поддерживает несколько режимов управления исполнительным механизмом. Режимы выстроены по приоритету: **Ручной режим → Режим управления сенсором/таймером**.

**Ручной режим (наивысший приоритет).** Включается немедленно после получения прямой команды пользователя по любому из каналов взаимодействия (MQTT или веб-интерфейс/zigbee итд) или при срабатывании таймера. В этом режиме показания датчика и таймер отложенного включения перестают влиять на состояние исполнытельного механизма. Таймер аварийного отключения продолжает работать. Данные датчиков продолжают публиковаться. Режим действует до явной команды возврата в режим управления сенсором или до перезагрузки устройства.

**Режим управления сенсором.** Основной (автоматический) режим. Исполнительный механизм включается при превышении хотя бы одного из верхних порогов: температуры **или** влажности. Выключение происходит только при одновременном снижении **обоих** показателей ниже нижних порогов. Пороги задаются в конфигурации устройства, но могут быть изменены пользователем оперативно (такие изменения сохраняются в EEPROM), но изменение самого режима управления не сохраняются (изменения действуют до выключения устройства).

**Режим управления таймером.** Предназначен для ситуаций, когда показания датчика малоинформативны (например, длительное использование туалета без значительных изменений влажности). Исполнительный механизм включается по прошествии заданного периода после подачи питания. Настраивается **только при конфигурации устройства**, может быть отключён установкой нулевого значения.

**Дополнительные возможности для типа 1:**

**Тихий режим (Slow Mode).** Снижает шум вентилятора за счёт импульсной подачи питания (ШИМ) с настраиваемой скважностью 0–100%. Может быть активирован при конфигурации устройства. Пользователь может оперативно включать/выключать этот режим и изменять скважность (изменения действуют до выключения устройства).

**Адаптивный тихий режим.** Работает только при активном тихом режиме. Автоматически увеличивает мощность вентилятора при ухудшении показателей окружающей среды (рост температуры или влажности), чтобы ускорить нормализацию микроклимата. Алгоритм адаптации задаётся жёстко при компиляции. Настраивается **при конфигурации устройства** (восстанавливается при включении питания), пользователь может включать/отключать адаптацию оперативно (изменения действуют до выключения устройства).

---

#### Для типа 2 (автономный датчик)

Устройство выполняет единственную функцию — периодический опрос датчика температуры и влажности и публикацию показаний по доступным каналам взаимодействия. Режимы управления исполнительным механизмом отсутствуют.

---

#### Для типа 3 (управляемый выключатель)

Устройство управляется только прямыми командами пользователя через каналы взаимодействия (ручной режим). Датчик отсутствует, режим управления сенсором и адаптивный тихий режим не применимы.

Доступные функции:
- **Режим управления таймером** — включение по прошествии заданного периода после подачи питания.
- **Таймер аварийного отключения** — принудительное отключение при превышении времени непрерывной работы.

## Технические описания и пояснения логики работы

### Логика работы

Устройство работает в двух режимах: **режим настройки** и **обычный режим**.

#### Первый запуск

При включении устройство пытается прочитать конфигурацию из энергонезависимой памяти:
- если параметры корректны — переход в **обычный режим**;
- если параметры отсутствуют или повреждены — переход в **режим настройки**.

## Режим настройки (Provisioning)

Устройство поддерживает 2 способа первоначальной настройки WiFi - BLE/AP WiFi.  Все методы передают **только SSID и пароль** WiFi-сети. После успешного подключения к WiFi все остальные параметры (MQTT, пороги, таймеры и т.д.) настраиваются через веб-интерфейс по IP-адресу устройства в локальной сети.

### Доступные методы настройки WiFi

| Метод | Описание | Требования |
|-------|----------|------------|
| **BLE** | Настройка через приложение ESP BLE Provisioning | Поддерживается на ESP32, ESP32-S3, ESP32-C3, ESP32-C6, ESP32-H2 |
| **AP** | Настройка через WiFi-точку доступа | Поддерживается на всех платформах |
| **AP+BLE** | Одновременная работа обоих методов | Требует ESP32 (кроме C3) |

### Процесс настройки

1. **Передача WiFi-параметров** (BLE или AP):
   - **BLE**: пользователь вводит SSID и пароль в приложении ESP BLE Provisioning.
   - **AP**: пользователь подключается к точке доступа `{prefix}_XXXX` и вводит SSID/пароль на странице `192.168.4.1`.

2. **Подключение к WiFi**: устройство получает SSID/пароль, подключается к указанной сети и получает IP-адрес.

3. **Окончательная настройка через Web** (в рабочей сети):
   - Пользователь открывает браузер по IP-адресу устройства (или через mDNS: `http://{prefix}-XXXX.local`).
   - Настройки устройства по умолчанию содержат данные полученные из файла `credentials.h` (при наличии).
   - Вводит (исправляет) все остальные параметры:
     - MQTT (брокер, порт, логин, пароль) — **всегда**, если выбран транспорт WiFi
     - Параметры устройства (пороги, таймеры, режимы) — согласно `DEVICE_TYPE`
   - Сохраняет настройки → устройство перезагружается и переходит в обычный режим.

> **Важно:** BLE и AP — это равноправные способы передать SSID/пароль. После подключения к WiFi процесс настройки идентичен для обоих методов.


#### Обычный режим

1. Чтение конфигурации из энергонезависимой памяти.
2. Инициализация датчика (если предусмотрен типом устройства).
3. Инициализация пина управления исполнительным механизмом (если предусмотрен).
4. Запуск основной логики, **не дожидаясь** подключения к каналам взаимодействия:
   - периодический опрос датчика;
   - управление исполнительным механизмом согласно приоритету режимов;
   - контроль таймера аварийного отключения;
5. Подключение к WiFi-сети по сохранённым параметрам, обслуживание веб-сервера.
6. При невозможности подключения в течение заданного тайм-аута — переход в **режим настройки**.
7. Подключение к существующему каналу взаимодействия:
- к MQTT-брокеру, публикация статуса `Online`
- обслуживание веб-сервера, публикация оперативного состояния
8. При потере соединения — автоматические попытки переподключения.
9. Устройство поднимает веб-сервер отображающий страницу состояния (опцилгально) и страницу настройки параметров. Также веб-сервер поддерживает станицу обновления прошивки по воздуху (опционально)

### Описание режимов и терминов

**Ручной режим.** Активируется внешней командой по каналам взаимодействия (кроме команд настройки параметров). Блокирует управление от датчика и таймера. Возврат — командой принудительного включения режима управления сенсором (если применимо) или перезагрузкой.

**Режим управления таймером.** Включение нагрузки по истечении `delaySeconds` после подачи питания. Работает **параллельно** с режимом управления сенсором (если включен): таймер может включить вентилятор независимо от показаний датчика, однако срабатывание таймера переводит управление в ручной режим, игнорирующий показания сенсора. Отключается командой ручного управления.

**Состояние исполнительного механизма при старте.** Параметр, определяющий положение реле сразу после подачи питания: включено/выключено. Настраивается при конфигурации.

**Тихий режим (Slow Mode).** ШИМ-управление с настраиваемой скважностью 0–100%. Стартовый импульс на полной мощности обеспечивает надёжный пуск вентилятора. Значения ниже минимального порога интерпретируются как команда выключения.

**Адаптивный тихий режим.** При активном тихом режиме отслеживает изменение показателей среды и динамически увеличивает скважность. При ошибке датчика переключает вентилятор на полную мощность. Ручное изменение скважности отключает адаптацию.

**Сохранение и отображение настроек.** Постоянные параметры сохраняются при получении соответствующих команд по каналам взаимодействия (MQTT/через веб-форму конфигурации). Оперативные изменения действуют до перезагрузки и не сохраняются в энергонезависимой памяти.

**Принудительный сброс настроек.** Выполняется длительным нажатием кнопки сброса, командой MQTT или через веб-интерфейс. Устройство удаляет все настройки заменяя их заводскими,перезагружается и переходит в режим настройки.

**Программный/Аппаратный Watch Dog.** Аппаратный или программный (в зависимости от hardware) таймер, перезагружающий устройство при зависании.

**Отображение причины перезагрузки.** При перезагрузке по ошибке причина публикуется в MQTT-топик `{prefix}/last_reset`. Штатные перезагрузки могут не публиковаться (настраивается при компиляции).

**Индикация состояния соединений.** Светодиод отображает факт срабатывания таймера аварийного отключения, режим точки доступа, текущий статус подключения к WiFi и MQTT: постоянное свечение — работа в штатном режиме, серии вспышек — нештатное состояние. При удержании кнопки сброса показывает время ужержания (серии вспышек)

---

### Состояние системы

Устройство использует **битовую маску** для хранения состояния (`SystemState`):

| Бит | Флаг | Описание |
|-----|------|----------|
| `1 << 0` | `STATE_BUTTON_PRESSED` | Кнопка сброса нажата |
| `1 << 1` | `STATE_WIFI_OK` | WiFi подключён |
| `1 << 2` | `STATE_MQTT_OK` | MQTT подключён |
| `1 << 3` | `STATE_PROVISIONING` | Режим настройки (AP/BLE) |
| `1 << 4` | `STATE_EMERGENCY` | Аварийное отключение |
| `1 << 5` | `STATE_RESTART` | Ожидание перезагрузки |

**API:**
- `system_state_set_bit(bit)` — установить флаг
- `system_state_clear_bit(bit)` — снять флаг
- `system_state_has_bit(bit)` — проверить флаг
- `system_state_get_bits()` — получить все флаги

**Принцип:** Каждый слой пишет свои флаги, другие слои читают их для принятия решений.

---

### Компоненты и зависимости

#### Основные библиотеки

| Библиотека | Назначение | Лицензия |
|------------|------------|----------|
| PubSubClient | MQTT-клиент | MIT |
| Adafruit AHTX0 | Датчик температуры/влажности AHT10 | BSD |
| DHT sensor library | Датчик DHT11/DHT22 | MIT |
| ElegantOTA | OTA-обновления через веб-интерфейс | MIT |
| BLEDevice (ESP32) | BLE-стек для комиссионинга | Apache 2.0 |

#### Структура проекта

см. `ARCHITECTURE.md`

## Техническая информация

### Индикация состояния соединений

Подробности в `LED_MANAGER.md`

### Кнопка сброса

Кнопка сброса управляется слоем `reset_btn`:

**Стадии нажатия:**
- `RELEASED` — отпущена
- `PRESSED` — нажата 0-1с
- `STAGE_1S` — нажата 1-2с
- `STAGE_2S` — нажата 2-3с
- `STAGE_3S` — нажата >3с

**Принцип:**
- `resetBtn_update()` вызывается в `loop()`
- При нажатии устанавливает `STATE_BUTTON_PRESSED`
- При отпускании снимает `STATE_BUTTON_PRESSED`
- Стадия нажатия хранится внутри `reset_btn`
- `resetBtn_get_stage()` вызывается только при `STATE_BUTTON_PRESSED`

### Менеджер перезагрузок

`restart_manager` обеспечивает централизованное управление перезагрузкой:

**API:**
- `restart_request(delayMs)` — запросить перезагрузку с задержкой
- `restart_update()` — проверка и выполнение перезагрузки (вызывается в loop)

**Особенности:**
- Защита от повторных запросов
- Логирование через `CAT_RESTART`
- Устанавливает `STATE_RESTART` в `SystemState`

### Каналы взаимодействия — описание и особенности

#### MQTT

MQTT — основной протокол интеграции устройства в экосистемы умного дома. Устройство подключается к заданному брокеру, публикует своё состояние и принимает команды управления.

**Принцип формирования топиков**

Все топики строятся по схеме: `{prefix}/{группа}/{параметр}`

Префикс (`prefix`) формируется при первом запуске на основе типа устройства и последних четырёх символов MAC-адреса:
- `fan_XXXX` — для типа 1 (вентилятор с датчиком)
- `sensor_XXXX` — для типа 2 (автономный датчик)
- `switch_XXXX` — для типа 3 (управляемый выключатель)

Топики управления (команды от брокера к устройству) формируются добавлением `c/` в путь: `{prefix}/c/{группа}/{параметр}`

---

**Топики только для публикации (устройство → брокер)**

Устройство публикует значения, управление извне не предусмотрено.

| Топик | Описание | Применимость |
|-------|----------|--------------|
| `{prefix}/status` | Статус устройства: `Online` / `Offline` (LWT) | Все типы |
| `{prefix}/sensor/temperature` | Текущая температура | Типы 1, 2 |
| `{prefix}/sensor/humidity` | Текущая влажность | Типы 1, 2 |
| `{prefix}/last_reset` | Причина последней перезагрузки | Все типы  |
| `{prefix}/version` | Версия прошивки | Все типы |
| `{prefix}/rssi` | Сила сигнала WiFi | Все типы |

---

**Топики только для управления (брокер → устройство)**

Устройство принимает команды, но не публикует текущее значение параметра.

| Топик | Описание | Применимость |
|-------|----------|--------------|
| `{prefix}/c/system/reset` | Сброс настроек к значениям по умолчанию | Все типы |

---

**Двунаправленные топики (публикация и управление)**

Устройство публикует текущее значение параметра и принимает команды на его изменение.

*Исполнительное устройство (типы 1, 3):*

| Топик | Описание | Применимость |
|-------|----------|--------------|
| `{prefix}/fan/state` | Состояние исполнительного механизма | Типы 1, 3 |
| `{prefix}/fan/delaySec` | Задержка отложенного включения | Типы 1, 3 |
| `{prefix}/fan/maxOnTime` | Время аварийного отключения | Типы 1, 3 |

**Примечание:** для типа 3 (выключатель) в топиках используется `switch` вместо `fan`.

*Режимы работы (только тип 1):*

| Топик | Описание |
|-------|----------|
| `{prefix}/fan/sensorControlMode` | Режим управления сенсором |
| `{prefix}/fan/adaptiveMode` | Адаптивный тихий режим |
| `{prefix}/fan/speed` | Текущая скважность ШИМ |

*Пороги датчика (только тип 1):*

| Топик | Описание |
|-------|----------|
| `{prefix}/sensor/lowTemp` | Нижний порог температуры |
| `{prefix}/sensor/highTemp` | Верхний порог температуры |
| `{prefix}/sensor/lowHum` | Нижний порог влажности |
| `{prefix}/sensor/highHum` | Верхний порог влажности |

---

**Особенности взаимодействия:**
- Прямая команда управления состоянием автоматически переводит устройство в ручной режим. Такие команды имеют оперативный характер — действуют до перезагрузки устройства.
- Команды изменения параметров устройсва, влияющих на логику работы приводят к сохранение параметров в энергонезависимую память.
- При выключении исполнительного механизма скважность ШИМ восстанавливается до сохранённого значения.
- При ручном изменении скважности адаптивный режим автоматически отключается до перезапуска устройсва.

### Режимы отладки и логирования

#### Уровни логирования

Уровень логирования задаётся макросом `XLOG_LEVEL`:

| Значение | Уровень | Описание |
|----------|---------|----------|
| 0 | NONE | Логирование отключено |
| 1 | ERROR | Только ошибки |
| 2 | WARN | Предупреждения + ERROR |
| 3 | INFO | Основные события (по умолчанию) |
| 4 | DEBUG | Все сообщения |

#### Категории логирования

| Категория | Описание |
|-----------|----------|
| CAT_CONFIG | Конфигурация |
| CAT_SENSOR | Датчик |
| CAT_FAN | Вентилятор |
| CAT_SWITCH | Выключатель |
| CAT_ACTUATOR | Исполнительный механизм |
| CAT_MQTT | MQTT |
| CAT_WIFI | WiFi |
| CAT_WEB | Web |
| CAT_OTA | OTA |
| CAT_LED | LED |
| CAT_WDT | Watchdog |
| CAT_AP | Точка доступа |
| CAT_MAIN | Основной цикл |
| CAT_PROVISIONING | Комиссионинг |
| CAT_RESTART | Перезагрузки |
| CAT_SYSTEM | Состояние системы |
| CAT_RESET_BTN | Кнопка сброса |

---

## Provisioning в режиме настройки

Режимы провизионинга:
```
0 - Нет
1 - Только BLE
2 - Только AP
3 - BLE + AP
```

### Поддерживаемые платформы и режимы

| Платформа | BLE | AP | PROVISIONING_METHOD |
|-----------|-----|----|---------------------|
| ESP8266 | ❌ | ✅ | 2 |
| ESP32 | ✅ | ✅ | 1 / 2 / 3 |
| ESP32-S2 | ❌ | ✅ | 2 |
| ESP32-S3 | ✅ | ✅ | 1 / 2 / 3 |
| ESP32-C3 | ✅ | ✅ | 1 / 2 / 3 |
| ESP32-C6 MQTT | ✅ | ✅ | 1 / 2 / 3 |
| ESP32-H2 MQTT | ✅ | ✅ | 1 / 2 / 3 |
| ESP32-C6 ZigBee | ❌ | ✅ | 0 или TRANSPORT_TYPE == TRANSPORT_TYPE_ZIGBEE |
| ESP32-H2 ZigBee | ❌ | ✅ | 0 или TRANSPORT_TYPE == TRANSPORT_TYPE_ZIGBEE |

### AP-Provisioning

Поднимается WiFi точка доступа.

### BLE-Provisioning

Поднимается BLE комиссионинг. Передаёт **только WiFi SSID и пароль**.

### Совместимые приложения

| Приложение | Платформа | Ссылка |
|------------|-----------|--------|
| **ESP BLE Provisioning** | Android | [Play Store](https://play.google.com/store/apps/details?id=com.espressif.bleprov) |
| **ESP BLE Provisioning** | iOS | [App Store](https://apps.apple.com/app/esp-ble-provisioning/id1473590141) |

---

### Zigbee (перспективный протокол)

**Статус:** Проектирование.

Поддерживаемые платформы: ESP32-C6, ESP32-H2.

### Matter (перспективный протокол)

**Статус:** Проектирование.

Поддерживаемые платформы: ESP32-C6, ESP32-H2.

---

### WEB-интерфейс

WEB-интерфейс предоставляет прямой доступ к настройке и управлению устройством через браузер.

| Режим | Адрес | Условия доступа |
|-------|-------|-----------------|
| Обычный режим | IP-адрес устройства | Устройство подключено к WiFi |
| Режим настройки (AP) | `192.168.4.1` | Устройство в режиме точки доступа |

**Страницы:**
- `/` — страница состояния
- `/config` — страница настройки
- `/save` — сохранение настроек
- `/update` — OTA-обновление (реализован средствами библиотеки)

## Описание слоев

### settings.h
Конфигуратор констант и их валидатор. Устанавливает правильные зависимости между слоями.

### system_state.h/cpp
**Состояние системы.** Битовая маска `uint16_t`. API: `set_bit`, `clear_bit`, `has_bit`, `get_bits`. Логирует изменения через `CAT_SYSTEM`.

### restart_manager.h/cpp
**Менеджер перезагрузок.** Централизованное управление перезагрузкой. Защита от повторных запросов, задержка перед `ESP.restart()`.

### config_manager.h/cpp
**Менеджер настроек.** Хранит значения в EEPROM, управляет загрузкой/сохранением, валидацией параметров.

### logger.h/cpp
**Система логирования.** Вывод в Serial с фильтрацией по уровню и категориям. Поддерживает цветной вывод.

### led_manager.h/cpp
**Светодиодная индикация.** Тупой исполнитель. Получает готовый режим через `led_set_mode()`.

### wdt_manager.h/cpp
**Watchdog таймер.** Аппаратный сторожевой таймер, защищающий от зависаний.

### reset_btn.h/cpp
**Кнопка сброса.** Обработка нажатий с определением длительности. Управляет `STATE_BUTTON_PRESSED`.

### sensor.h/cpp
**Датчик температуры и влажности.** Чтение с AHT10 (I2C) или DHT11/DHT22.

### actuator_base.h/cpp
**Базовый класс исполнительного механизма.** Управление реле, таймеры.

### fan_actuator.h/cpp
**Управление вентилятором (тип 1).** ШИМ, адаптивный режим, стартовый импульс.

### switch_actuator.h/cpp
**Управление выключателем (тип 3).** Простое включение/выключение реле.

### wifi_manager.h/cpp
**WiFi-подключение.** STA и AP режимы, мониторинг, fallback. Управляет `STATE_WIFI_OK`.

### mqtt_manager.h/cpp
**MQTT-клиент.** Подключение к брокеру, публикация, приём команд. Управляет `STATE_MQTT_OK`.

### web_manager.h/cpp
**Веб-интерфейс.** HTTP-сервер для управления и настройки.

### ota.h/cpp
**OTA-обновления.** Обновление прошивки через веб-интерфейс.

### provisioning_manager.h/cpp
**Менеджер комиссионинга.** Управление BLE и AP настройкой. Управляет `STATE_PROVISIONING`.

### ble_server.h/cpp
**BLE-сервер.** ESP BLE Provisioning для ESP32.

### main.cpp
**Оркестратор.** Точка входа. Инициализирует все слои, вызывает их в loop. Единственный, кто вызывает `ConfigManager::save()` и `ESP.restart()`. Содержит логику определения режима LED.

---

## Архитектура слоёв
Подробности в `ARCHITECTURE.md`


## ДОКУМЕНТАЦИЯ ПРОЕКТА

### Чек-лист готовности 

#### Документация слоев
**Размещение** dev_doc/

|Статус|Документ|Описание|
|-|-|-|
|[В процессе]| PROJECT.md |Этот файл|
|[На удаление]|ADR.md |Глобальные требования к архитектурному базису и контрактам компонентов|
|[В процессе]|ARCHITECTURE.md | Описание архитектуры|
|[В процессе]|ORCHESTRATOR.md | Оркестратор (`main.cpp`)|

|[В процессе]|ACTUATOR_SENSOR.md  ||            
|[Закончен]|DEVICE_TYPES.md  ||               
|[Закончен]|SYSTEM_STATE.md     ||            

|[Закончен]|TRANSPORT.md||
|[В процессе]|TRANSPORT_ABSTRACTION.md| |
|[В процессе]|TRANSPORT_ZIGBEE.md||
|[В процессе]|TRANSPORT_MATTER.md||
|[В процессе]|TRANSPORT_MQTT.md||

|[В процессе]|BUILD_AND_CONFIG.md  ||           
|[Закончен]|LOGGER.md    | Логирование событий |           
|[Закончен]|LED_MANAGER.md |Светодиодная индикация|          

|[В процессе]|CONFIG_AND_STATE.md||             
|[В процессе]|OTA_MANAGER.md   | Обновление про|                      

|[В процессе]|DEVICE_CONTROLLER.md  ||          
|[В процессе]|PROVISIONING_MANAGER.md |Провизионинг (первоначальная настройка)|               
|[В процессе]|TRANSPORT_WEB.md||
|[Закончен]|DEBUG_TOOLS.md  | Вспомогательные функции для диагностики|
|[В процессе]|FIXME.md  | Технический долг|

#### Спецификации
**Размещение** dev_doc/specifications
|Статус|Документ|Описание|
|-|-|-|
|[В процессе]|scpec_matter.md  ||       
|[В процессе]|spec_logger.md   ||       
|[В процессе]|spec_mqtt.md    ||        
|[В процессе]|spec_reset.md    ||       
|[В процессе]|spec_zigbee.md||
|[В процессе]|spec_led.md   ||          
|[В процессе]|spec_matter.md  ||        
|[В процессе]|spec_ota.md    ||         
|[В процессе]|spec_web_interface.md||