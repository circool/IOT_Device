# TRANSPORT_ZIGBEE.md

## Zigbee-транспорт

---

## 1. Назначение

Zigbee — протокол для построения ячеистых (mesh) сетей с низким энергопотреблением. Устройство подключается к координатору сети (Zigbee-шлюзу) и представляет себя как одно или несколько логических устройств (endpoints) со стандартизированными кластерами.

**Статус:** ⏳ Проектирование. Реализация отсутствует, существует только заглушка (`transport_zigbee.h/cpp`). Ожидает ESP-Zigbee-SDK.

---

## 2. Реализация

### 2.1. Файлы

| Файл | Назначение | Статус |
|------|------------|--------|
| `transport_zigbee.h/cpp` | Реализация Zigbee-транспорта | ⏳ Заглушка |
| `transport.h` | Интерфейс `Transport` | ✅ Реализован |

### 2.2. Поддерживаемые платформы

| Платформа | Поддержка Zigbee |
|-----------|------------------|
| ESP32-C6 | ✅ Да (аппаратная поддержка) |
| ESP32-H2 | ✅ Да (аппаратная поддержка) |
| ESP32, ESP8266 | ❌ Нет |

### 2.3. Отличия от MQTT

| Характеристика | MQTT | Zigbee |
|----------------|------|--------|
| **Тип сети** | Клиент-сервер (TCP/IP) | Ячеистая (mesh) |
| **Адресация** | Топики (строки) | Endpoint + Cluster + Attribute |
| **Публикация** | Периодическая или по событию | Отчёты атрибутов (Reporting) |
| **Команды** | Через топики `{prefix}/c/...` | Через ZCL-команды |
| **Управление** | Брокер | Координатор или прямой биндинг |
| **Формат данных** | JSON или plain text | ZCL (структурированный) |

---

## 3. Принцип адресации

Устройство идентифицируется в сети по:
- **64-битному MAC-адресу** (IEEE-адрес) — уникальный
- **16-битному сетевому адресу** (NwkAddr) — назначается координатором

Взаимодействие строится по модели:

```
{Endpoint} → {Cluster} → {Attribute}
```

---

## 4. Конечные точки (Endpoints)

Устройство представляет **три конечные точки**:

| Endpoint | Назначение | Применимость |
|----------|------------|--------------|
| **1** | Управление вентилятором / выключателем | TYPE 1, 3 |
| **2** | Датчик температуры и влажности | TYPE 1, 2 |
| **3** | Конфигурация устройства | Все типы |

---

## 5. Кластеры и атрибуты

### 5.1. Endpoint 1 — Управление (TYPE 1, 3)

| Кластер | ID | Тип | Атрибуты |
|---------|-----|-----|----------|
| **On/Off** | 0x0006 | Сервер | `OnOff` (bool) — состояние актуатора |
| **Level Control** | 0x0008 | Сервер | `CurrentLevel` (0-100%) — скорость ШИМ |
| **Fan Control** | 0x0202 | Сервер | `FanMode` — режим работы; `FanModeSequence` — адаптивный режим |

**Команды (Endpoint 1):**

| Кластер | Команда | Параметр | Назначение |
|---------|---------|----------|------------|
| On/Off | `On` | — | Включить |
| On/Off | `Off` | — | Выключить |
| On/Off | `Toggle` | — | Переключить состояние |
| Level Control | `MoveToLevel` | `level` (0-100) | Установить скорость |

### 5.2. Endpoint 2 — Датчик (TYPE 1, 2)

| Кластер | ID | Тип | Атрибуты |
|---------|-----|-----|----------|
| **Temperature Measurement** | 0x0402 | Сервер | `MeasuredValue` — температура (°C × 100) |
| **Relative Humidity Measurement** | 0x0405 | Сервер | `MeasuredValue` — влажность (% × 100) |

**Примечание:** Zigbee использует масштабированные значения: температура × 100, влажность × 100.

### 5.3. Endpoint 3 — Конфигурация (Все типы)

| Кластер | ID | Тип | Атрибуты |
|---------|-----|-----|----------|
| **Basic** | 0x0000 | Сервер | `ManufacturerName`, `ModelIdentifier`, `SWBuildID` |
| **OTA Upgrade** | 0x0019 | Клиент | Обновление прошивки |
| **Custom (пользовательский)** | 0xFC00 | Сервер | Пороги, таймеры, режимы |

**Пользовательский кластер (0xFC00):**

| Атрибут | ID | Тип | Назначение |
|---------|-----|-----|------------|
| `LowTemp` | 0x0000 | int16 | Нижний порог температуры (°C × 100) |
| `HighTemp` | 0x0001 | int16 | Верхний порог температуры (°C × 100) |
| `LowHum` | 0x0002 | uint16 | Нижний порог влажности (% × 100) |
| `HighHum` | 0x0003 | uint16 | Верхний порог влажности (% × 100) |
| `DelaySeconds` | 0x0004 | uint16 | Задержка включения (сек) |
| `MaxOnTime` | 0x0005 | uint32 | Аварийное отключение (сек) |
| `SensorControlMode` | 0x0006 | bool | Режим AUTO / MANUAL |
| `AdaptiveMode` | 0x0007 | bool | Адаптивный режим |
| `BootState` | 0x0008 | bool | Состояние при старте |
| `SpeedPercent` | 0x0009 | uint8 | Скорость (0-100%) |

---

## 6. Типы взаимодействия

| Кластер | Публикация | Управление | Механизм |
|---------|-----------|------------|----------|
| **On/Off** | ✅ Да | ✅ Да | Отчёты атрибутов + команды `On`/`Off` |
| **Level Control** | ✅ Да | ✅ Да | Отчёты + команда `MoveToLevel` |
| **Fan Control** | ✅ Да | ✅ Да | Отчёты + `Write Attributes` |
| **Temperature Measurement** | ✅ Да | ❌ Нет | Периодические отчёты |
| **Relative Humidity** | ✅ Да | ❌ Нет | Периодические отчёты |
| **Basic** | ✅ Да | ❌ Нет | Статические атрибуты |
| **OTA Upgrade** | ❌ Нет | ✅ Да | Приём обновлений |
| **Custom (0xFC00)** | ✅ Да | ✅ Да | Отчёты + `Write Attributes` |

---

## 7. Отчёты атрибутов (Reporting)

Атрибуты, отмеченные как «Публикация: Да», поддерживают механизм отчётов:

1. Устройство настраивает периодичность отчётов при старте
2. Координатор подписывается на атрибуты
3. При изменении атрибута устройство отправляет отчёт
4. Отчёты также отправляются периодически (heartbeat)

**Периодичность отчётов:**

| Атрибут | Минимальный интервал | Максимальный интервал |
|---------|---------------------|---------------------|
| Температура | 5 сек | 300 сек |
| Влажность | 5 сек | 300 сек |
| Состояние (On/Off) | 0 (при изменении) | 60 сек |
| Скорость | 0 (при изменении) | 60 сек |

---

## 8. Связь с MQTT-транспортом (сравнение)

| Аспект | MQTT | Zigbee |
|--------|------|--------|
| **Команда включения** | `{prefix}/c/fan/state` → `ON` | Endpoint 1 → Cluster 0x0006 → Command `On` |
| **Публикация состояния** | `{prefix}/fan/state` → `ON` | Endpoint 1 → Cluster 0x0006 → Attribute `OnOff` → Report |
| **Команда скорости** | `{prefix}/c/fan/speed` → `75` | Endpoint 1 → Cluster 0x0008 → Command `MoveToLevel(75)` |
| **Публикация температуры** | `{prefix}/sensor/temperature` → `24.5` | Endpoint 2 → Cluster 0x0402 → Attribute `MeasuredValue` → Report |
| **Настройка порога** | `{prefix}/c/sensor/lowTemp` → `27.0` | Endpoint 3 → Cluster 0xFC00 → Write `LowTemp` |

---

## 9. Требования к реализации

Для полноценной реализации Zigbee-транспорта необходимо:

| # | Задача | Приоритет |
|---|--------|-----------|
| 1 | Инициализация Zigbee-стека (ESP-Zigbee-SDK) | 🔴 Критично |
| 2 | Регистрация endpoints (1, 2, 3) | 🔴 Критично |
| 3 | Реализация кластеров и атрибутов | 🔴 Критично |
| 4 | Реализация отчётов атрибутов (Reporting) | 🟠 Высокий |
| 5 | Обработка входящих ZCL-команд | 🟠 Высокий |
| 6 | Реализация OTA через Zigbee | 🟡 Средний |
| 7 | Интеграция с `Transport` интерфейсом | 🟡 Средний |

---

## 10. Флаги компиляции

| Флаг | Значение по умолчанию | Описание |
|------|----------------------|----------|
| `TRANSPORT_TYPE` | `2` (для ESP32-C6/H2) | Выбор Zigbee-транспорта |
| `FEATURE_ZIGBEE_ENABLED` | `1` (для ESP32-C6/H2) | Включить Zigbee-стек |

---

## 11. Связь со спецификацией

Данный документ описывает **реализацию** Zigbee-транспорта в рамках структуры `Transport`.

**Требования** к Zigbee-интерфейсу (внешнее поведение, кластеры, атрибуты) описаны в отдельном документе:

→ **`spec_zigbee.md`**

---

